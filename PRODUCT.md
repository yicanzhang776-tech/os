# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

本科操作系统课程学生是高频使用者，需要在实验过程中定位当前 Lab、运行内核、检查真实证据并获得分级提示。教师与比赛评委需要用同一套真实数据进行课堂讲解、答辩展示和结果复核。

## Product Purpose

在 Rust、RISC-V 64、QEMU 和 OpenSBI 教学环境中，把 P0-Lab7 的代码、运行事件、状态变化、诊断、回放和 AI 引导组织为一套可解释、可验证的实验工作台。成功意味着使用者能迅速回答“运行到哪里、改变了什么、为什么与前后实验相连”。

## Positioning

页面不播放预制动画，而是将当前 Git 分支、真实构建结果、QEMU 结构化事件和受限智能体工具同步到同一教学视图；任何完成状态和智能体建议都必须能回到代码与运行证据验证。

## Operating Context

页面由仓库内无构建依赖的 Node bridge 在本机提供。学生通常在编辑器、终端与浏览器之间往返；教师和评委会在投影或全屏环境中讲解保存的运行记录。网络和模型服务可能不可用，本地实验、诊断、回放与比较仍需完整工作。

## Capabilities and Constraints

- 保持原生 HTML、CSS、JavaScript，不引入前端构建链或运行时 CDN。
- 保持现有 `/api/context`、`/api/agent`、WebSocket 和 `os-demo.*` 协议。
- 支持学习与演示两种同级模式，以及 P0-Lab7、starter/solution、预测、运行、诊断、回放、比较、导入导出和自愿反馈。
- 智能体请求由本地服务端持有密钥并限制为六类白名单工具；回答不是标准答案、根因判定或评分依据。
- 界面必须在中文、长技术文本、未配置模型、离线和窄屏环境下可用。

## Brand Commitments

产品名称为“AI 合作的操作系统教学实验环境”。语言直接、教学化、基于证据，不使用营销式夸张，不把不确定判断写成结论。

## Evidence on Hand

仓库已有 P0-Lab7 教学内容、真实事件协议、状态模型、诊断规则、运行记录、分支比较、教师评分材料和 AI 工具边界。不存在需要在界面中虚构的用户评价、获奖结果或性能数据。

## Product Principles

- 真实证据先于视觉演示。
- 当前任务与下一步操作必须在首屏可见。
- 本地确定性能力不依赖模型服务。
- 学习模式强调操作效率，演示模式强调讲解顺序，两者使用同一事实来源。
- 数据发送必须显式、自愿、最小化且可解释。

## Accessibility & Inclusion

支持键盘操作、可见焦点、减少动画、中文长文本、200% 缩放和常见桌面/移动视口；状态不能只依赖颜色表达。
